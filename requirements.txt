requests          # Ollama HTTP calls
python-dotenv     # load .env
rich              # pretty terminal output
psutil            # CPU/RAM stats
chromadb          # local vector DB
pypdf             # PDF loading
tiktoken          # token counting for chunking
faster-whisper    # STT (needs ffmpeg on PATH)
sounddevice       # mic input
numpy             # audio array handling
fastapi           # Web framework(API Layer
uvicorn           # ASGI server(API Layer)
pyttsx3           # TTS (text-to-speech)
ddgs==9.5.5      # DuckDuckGo search for live web/news queries
sentence-transformers # local embeddings for RAG
ddgs              # DuckDuckGo search for live web/news queries
streamlit